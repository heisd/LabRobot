# LabRobot 项目工作总结 (2026-06-14)

## 1. 语音控制模块 (`wheeltec_mic_ros2`)
*   **功能增强**：重构 `voice_control` 节点，支持通过 `audio_device` 参数切换音频采集设备，实现有线麦克风阵列与无线蓝牙设备的兼容。
*   **硬件说明**：
    *   明确本机器人默认**未内置蓝牙板卡**，需用户自行扩展。
    *   添加**免责声明**：蓝牙功能未经过闭环认证，属于实验性支持。
*   **启动优化**：更新 `mic_init.launch.py`，支持 `use_bluetooth` 参数切换。

## 2. 摄像头模块 (`astra_camera` & `usb_cam`)
*   **Astra/Gemini 修复**：
    *   **ID 匹配**：将 UVC 彩色端的 Product ID 从 `0x0501` 修正为 **`0x0511`**，解决了找不到设备导致的崩溃。
    *   **权限修复**：记录了运行 `scripts/install.sh` 安装 udev 规则的必要性。
    *   **文档补充**：在 README 中增加了如何使用 RViz2 查看点云的说明（需设置 Reliability 为 Best Effort），并明确图像保存路径为 `astra_camera/OutputPicture/`。
*   **usb_cam 修复**：
    *   修正了默认设备路径，从不存在的 `/dev/Astra_Gemini` 切换为实际存在的 **`/dev/video0`**，解决了启动异常。

## 3. 雷达融合模块 (`double_lidar_fusion`)
*   **状态记录**：在文档中正式记录了 **雷达 1 (Lidar 1) 暂时故障** 的现状。
*   **运行逻辑**：明确了节点会自动检测故障并降级为单雷达（仅雷达 2）工作的模式。

## 4. 大模型对话模块 (`ollama_ros_chat`)
*   **环境确认**：更新文档，明确本机器人已**预装 Ollama 运行环境**及 `qwen2:1.5b` 模型。
*   **安装指引**：补充了手动安装 Ollama 的官方命令。

## 5. 其他系统说明
*   **MSC 目录**：确认根目录下 `msc/` 文件夹为科大讯飞 SDK 的运行时授权、配置与缓存目录，不可删除。

---
**当前机器人状态自检：**
- [x] **3D 相机**：Astra/Gemini 驱动正常，彩色与深度流可用。
- [x] **语音系统**：支持有线/无线切换，SDK 授权有效。
- [!] **激光雷达**：雷达 1 离线，雷达 2 正常，融合节点已自动降级。
- [x] **本地大模型**：Ollama 服务后台运行中。
