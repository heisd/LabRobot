# WSL 仿真平台修复记录

> 日期: 2026-06-12
> 环境: WSL2 (Ubuntu 22.04) + ROS 2 Humble + Gazebo Classic 11.10.2 + WSLg
> 症状: Dashboard 启动机械臂抓取仿真(`ros2 launch lebai_gazebo gazebo_grab.launch.py`)时,
> Gazebo 窗口短暂黑屏后闪退; `imu_filter_madgwick` 一直等 `imu/data_raw`(此为底盘链路噪音, 与本问题无关)。

最终结论: **不是 WSL 显卡/显示的问题**(WSLg 的 DISPLAY 与 GPU 加速均正常), 而是一连串
代码/配置/环境问题叠加。从外到内共修复 9 个问题, 现已端到端打通:
控制器激活成功、相机出图、抓取节点全部就绪。

---

## 问题与修复清单(按发现顺序)

### 1. 黑屏: GAZEBO_RESOURCE_PATH 丢失系统路径
- **现象**: gzclient 窗口全黑, 日志 `[Err] [RTShaderSystem.cc:480] Unable to find shader lib`。
- **原因**: wheeltec_gazebo 的环境钩子 prepend 了 `GAZEBO_RESOURCE_PATH`, 但环境里原本没有
  该变量, 系统默认的 `/usr/share/gazebo-11`(Ogre shader 库所在)没有进去。
- **修复**: 启动仿真前 `source /usr/share/gazebo/setup.sh`(建议写入 `~/.bashrc`,
  且必须在启动 Dashboard 的终端生效, 因为 sim_launcher 继承父进程环境)。

### 2. 12 秒闪退: grab_demo 包从未编译
- **现象**: Gazebo 起来约 12 秒后整个 launch 被拉倒。
- **原因**: `gazebo_grab.launch.py` 的 `TimerAction(12s)` 要启动 `grab_demo` 的节点,
  但该包不在 `install/` 里, launch 抛 `package 'grab_demo' not found` 异常 → 全部进程被 SIGINT。
  sim_launcher 把子进程输出全部丢到 DEVNULL, 所以 Dashboard 侧看不到这条致命报错。
- **修复**: 编译该包(过程中又揪出 3 个编译问题, 见 3~5)。

### 3. 编译失败: Windows MSYS2 头文件污染 (WSL 特有)
- **现象**: octomap 头文件报 `'floor' was not declared in this scope`。
- **原因**: WSL 默认把 Windows PATH 拼进 Linux PATH, CMake 顺着
  `/mnt/c/msys64/ucrt64/bin` 找到了 **Windows 的 CURL**(`CURL_DIR=/mnt/c/msys64/ucrt64/lib/cmake/CURL`),
  其 include 目录里 MinGW 的 `math.h` 遮蔽了 glibc 头文件。
- **修复**: 删除被污染的 `build/grab_demo` 缓存; 编译时剥离 Windows 路径:
  ```bash
  export PATH=$(echo "$PATH" | tr ":" "\n" | grep -v "^/mnt/" | paste -sd:)
  colcon build ...
  ```
  注意 PATH 里还有 `/mnt/f/opencv/...`(Windows OpenCV), 是同类地雷, 建议每次编译都带上这行。

### 4. 编译失败: grab_demo/package.xml 依赖缺失
- **现象**: `find_package(lebai_interfaces)` 找不到。
- **原因**: CMakeLists 用了 lebai_interfaces / MoveIt / pcl_ros / nav2_msgs 等十余个包,
  package.xml 一个都没声明, colcon 不知道要注入同工作区依赖的路径、也无法保证编译顺序。
- **修复**: `src/grab_demo/package.xml` 补全全部 `<depend>`。

### 5. 编译失败: 写死的 OpenCV 4.10 路径 + 新 API 不兼容
- **现象**: `fatal error: /usr/local/opencv410/include/... No such file`。
- **原因**: `aruco_dectet.cpp` / `charuco_dectet_node.cpp` 写死了原作者机器上自编译
  OpenCV 4.10 的绝对路径; 且代码用的是 OpenCV ≥4.7 的新 ArUco 类 API
  (`cv::aruco::ArucoDetector` 等), 本机系统 OpenCV 4.5.4 没有。
- **修复**: 绝对路径改为标准 `#include <opencv2/objdetect.hpp>`;
  CMakeLists 把这两个节点改成**条件编译**(检测到新 API 头文件才编, 同包内 YOLO 缺 CUDA
  跳过、KCF 缺 tracking 跳过是同一惯例)。仿真抓取只用 hsv_range + grab_service_node, 不受影响。

### 6. controller_manager 起不来: URDF 注释撑爆 rcl 参数解析
- **现象**: `[gazebo_ros2_control] parser error Couldn't parse parameter override rule:
  '--param robot_description:=<?xml ...'`, spawner 永远等不到 `/controller_manager`。
- **原因**: gazebo_ros2_control 把整串 URDF 以 `--param robot_description:=<urdf>` 传给
  controller_manager, 而 **rcl 参数解析器遇到值里的「冒号+空格」会直接失败**
  ——URDF 注释里的中文说明正好有("...所需: ")。已实验证实: `a: b` 解析失败, 纯中文/换行没问题。
- **修复**: `lebai_gazebo/launch/gazebo.launch.py` 改为 Python 侧展开 xacro 并
  `re.sub(r'<!--.*?-->', '', urdf)` 剥掉全部 XML 注释后再发布 robot_description。

### 7. 机械臂模型在 Gazebo 里是"空模型": 根链路无 inertial
- **现象**: `Skipping joint ... which is not in the gazebo model` ×6,
  `gz model --info` 显示模型只剩一个 plugin, 没有任何 link/joint; 相机也不出图。
- **原因**: `base_link` 与 `lebai_base_link` 都没有 `<inertial>`, URDF→SDF 转换把
  **整个模型静默丢弃**(离线 `gz sdf -p` 复现: 补 inertial 前 0 个 link, 补后全部恢复)。
- **修复**: `lebai_lm3_support/urdf/lm3_macro.xacro` 给两个链路补上质量与惯量。
  (注: robot_state_publisher 会警告 KDL 不支持根链路惯量, 无害, 可忽略。)

### 8. world 模型写死别人机器的绝对路径 + 网格解析卡死在线模型库
- **现象 A**: `Unable to find uri[model:///home/user/.gazebo/models/table]` ——
  桌子/可乐罐/地面全部加载失败, 画面空空。
- **现象 B**: 修完 A 之后 gzserver 卡死在 `Waiting for model database update to complete...`,
  ros2_control 插件永远轮不到加载。原因: URDF 的 `package://lebai_lm3_support/meshes/...`
  转 SDF 后变成 `model://lebai_lm3_support/...`, GAZEBO_MODEL_PATH 里解析不到,
  gzserver 就去请求**已停运的** models.gazebosim.org, 模型加载线程被阻塞。
- **修复**:
  - 12 个 world 文件(lebai 6 + wheeltec 6)的 `model:///home/user/...` 全部改为 `model://XXX` 相对 URI;
  - 给 `lebai_lm3_support` 新增环境钩子(`hooks/*.dsv.in` + `ament_environment_hooks`),
    把其 `share` 目录注册进 GAZEBO_MODEL_PATH, 网格本地解析;
  - 把 lebai 各 world 用到的 9 个场景模型(table/coke_can/beer/wood_cube_5cm/bowl/
    hammer/plastic_cup/box_target_green/box_target_red, 共约 5.5MB)从 `~/.gazebo/models`
    拷进 `src/lebai_gazebo/models/`, 机械臂仿真自此**完全自包含**, 不依赖任何环境变量与联网。

### 9. 桌子布局不合理(用户确认"桌子有点高")
- **原因**: 标准 table 桌面 1.0m 高; 臂肩部仅 0.47m、最大可达半径约 0.62m, 桌上物体距肩
  0.74m 够不着。且桌子长边(1.5m)朝臂、桌板横跨 x∈[-0.2, 1.3], 把原点处的机械臂罩在桌底。
- **修复**: 6 个 grab world 统一: 桌子下沉 0.45m(桌面 0.55m)+ 旋转 90°(短边朝臂),
  物体降到 0.565m。后续第 11 条又把桌子外移到 x=0.75(桌沿 0.35), 物体距肩 0.46~0.65m,
  在抓取可达区内。

### 10. 机器人出生即被弹飞("机器人飞出去/模型消失")
- **现象**: 模型修好后(问题 7), 机器人 spawn 几秒内被弹射到视野外, 看起来像"没有模型"。
- **原因**: 车底座碰撞网格 `s300_car_base_link.stl` 的 z 范围是 **-0.035 ~ 0.256**
  (轮子在 base_link 原点之下), spawn 在 z=0 时轮子穿进地面 3.5cm, ODE 用巨大接触力弹飞整车。
  之前模型是空的(问题 7), 没有碰撞体, 所以从未暴露。
- **修复**: `lm3_gazebo.xacro` 加 `<link name="world"/>` + fixed joint(z=0.035),
  把底座抬到轮子贴地并**锚定到世界**(抓取演示不需要底盘移动, 锚定后臂运动反作用力也
  不会推车漂移)。同时删除 `gazebo_grab.launch.py` 里的恒等 world->base_link 静态 TF
  (改由 robot_state_publisher 按 URDF 发布, 与 Gazebo 锚定位置一致)。
  验证: `gz model --pose` 恒为 0 0 0, 12 秒不漂移。

### 11. 旋转后的桌板压住机械臂("被桌子压着")
- **现象**: 桌子转 90° 后桌沿(x=0.15)伸到机械臂头顶, 0.55m 的桌板正好压进停泊姿态的
  肘部(约 0.7m 高), 物理引擎狂算穿模接触, RTF 掉到 0.2。
- **修复**: 桌心 x 0.55 → 0.75(桌沿退到 0.35), 让开臂的停泊包络(x≲0.3)与车体(x≤0.363,
  但低于桌面无碰撞); 桌上物体不动, 仍在桌面且可达。6 个 world 统一修改。

### 12. WSL 偶发: OpenAL 探测音频卡 30s, spawn 超时("没有渲染起来")
- **现象**: gzserver 卡在 world 加载 31 秒(日志: `Loading world file` 后长时间无输出,
  然后 `[Err] [OpenAL.cc:84] Unable to open audio device[default]`), 而 spawn_entity
  默认 30 秒超时, 正好在 world 就绪前 2 秒放弃 → 场景里只有空桌子没有机器人。
- **原因**: WSLg 的 PulseAudio 偶发无响应, Gazebo 启动时 OpenAL 探测设备被拖死。
- **修复**: `gazebo.launch.py` 里 `SetEnvironmentVariable('ALSOFT_DRIVERS', 'null')`
  强制 OpenAL 走 null 后端跳过设备探测(仿真不需要声音); spawn_entity 加 `-timeout 60` 兜底。

---

## 修改文件清单

| 文件 | 改动 |
|---|---|
| `src/grab_demo/package.xml` | 补全缺失的依赖声明 |
| `src/grab_demo/CMakeLists.txt` | aruco/charuco 节点改条件编译(OpenCV≥4.7 才编) |
| `src/grab_demo/src/aruco_dectet.cpp` | 写死的 opencv410 路径 → 标准 include |
| `src/grab_demo/src/charuco_dectet_node.cpp` | 同上 |
| `src/lebai_gazebo/launch/gazebo.launch.py` | robot_description 改 Python 展开并剥 XML 注释 |
| `src/lebai-ros-sdk-humble-dev/lebai_lm3_support/urdf/lm3_macro.xacro` | base_link / lebai_base_link 补 inertial |
| `src/lebai-ros-sdk-humble-dev/lebai_lm3_support/CMakeLists.txt` | 新增 GAZEBO_MODEL_PATH 环境钩子 |
| `src/lebai-ros-sdk-humble-dev/lebai_lm3_support/hooks/lebai_lm3_support.dsv.in` | 新建(钩子定义) |
| `src/lebai_gazebo/worlds/*.world` (6 个) | model:// 相对 URI; 桌子下沉+旋转; 物体降高 |
| `src/wheeltec_gazebo/worlds/*.world` (6 个) | model:// 相对 URI; 清理过时注释 |
| `src/lebai_gazebo/models/` | 新增 9 个场景模型(自包含) |

## 环境配置(建议写入 ~/.bashrc)

```bash
source /opt/ros/humble/setup.bash
source ~/Lab/install/setup.bash
source /usr/share/gazebo/setup.sh                              # 修黑屏(shader 资源路径)
export GAZEBO_MODEL_PATH=$HOME/.gazebo/models:$GAZEBO_MODEL_PATH  # wheeltec 世界的模型仍在这里
```

注意: **Dashboard / sim_launcher 继承启动它的终端环境**, 以上必须在启动 Dashboard 之前生效。

编译命令建议(剥离 Windows 路径, 防 /mnt/c、/mnt/f 工具链污染):

```bash
cd ~/Lab
PATH=$(echo "$PATH" | tr ":" "\n" | grep -v "^/mnt/" | paste -sd:) colcon build --packages-up-to grab_demo
```

## 验证结果(2026-06-12)

100 秒端到端运行 `gazebo_grab.launch.py world:=grab_world`, 不依赖任何手动环境变量:

- gazebo_ros2_control 插件加载成功
- `joint_state_broadcaster`、`lebai_trajectory_controller` 均 **Configured and activated**
- 眼在手相机 `/camera_arm/color|depth|points` 正常发布, hsv_range 收到图像(0 次断流警告)
- 0 进程崩溃、0 模型加载错误、0 在线模型库等待

## 遗留事项 / 建议

1. **sim_launcher 吞日志**: `wheeltec_dashboard/sim_launcher.py:150-153` 把子进程 stdout/stderr
   全部重定向 DEVNULL, 本次一连串问题在 Dashboard 侧完全不可见。建议改为写入
   `~/.ros/sim_<key>.log` 之类的文件。
2. **sim_launcher 停止不彻底**: gzserver 收 SIGINT 经常退不干净(本次实测需 SIGKILL),
   残留进程会占住 11345 端口, 导致下次启动 `gzserver exit code 255`。建议 stop 逻辑加
   "SIGINT → 等待超时 → SIGKILL" 升级, 或启动前清理残留 gzserver。
   另: gzserver 关停时偶发 Ogre 断言崩溃(`OgreAxisAlignedBox.h:251 Assertion failed`,
   exit code -6), 为 Gazebo Classic 在 WSL 下的已知退出毛病, 只发生在关闭瞬间,
   不影响运行中的仿真, 可忽略。
3. **wheeltec 世界的模型未打包**: bookshelf/cafe_table/person_standing 等 15 种模型仍依赖
   `~/.gazebo/models`(已在环境配置里兜底)。如需自包含可仿照 lebai_gazebo 拷入包内。
4. aruco/charuco 检测节点在本机(OpenCV 4.5.4)不编译; 如需启用, 装 OpenCV ≥4.7 或把代码
   移植回 contrib 旧 API。
